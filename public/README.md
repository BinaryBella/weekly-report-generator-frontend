# Static assets

Files here are served from the site root (`/`).

## Required

- `login-illustration.png` — the illustration shown on the left panel of the
  login page (`src/app/login/page.tsx`). Save the "person signing in on a
  laptop" image here with exactly this name. A transparent PNG works best.
